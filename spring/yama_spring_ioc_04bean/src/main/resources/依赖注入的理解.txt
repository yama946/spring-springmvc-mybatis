对于ioc对象的创建于依赖注入
对于使用bean标签,主要是将一个对象通过，构造方法，或者其他两种方式，创建一个对象存放到容器中。
此时容器中仅仅时存在这么一个对象，而此对象中如果依赖其他对象进行功能操作，还需要进行依赖注入

所谓依赖注入，就是为容器中的对象，提供其依赖的对象，
这个对象可以时基本类型，也可以是容器中的类型，我们都可通过配置为其提供对象。

依赖注入再理解；
无论是通过构造方法进行的依赖注入，还是通过set方法，还是注解进行的数据注入，是
将ioc容器中的数据进行获取，为对象中的相应变量进行的初始化。，否则变量为null